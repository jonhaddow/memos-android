<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/memoListRow"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingEnd="@dimen/activity_horizontal_margin"
    android:paddingStart="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <TextView
        android:id="@+id/tvMemoContent"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:textSize="20sp"
        tools:text="Hello" />

    <ImageView
        android:id="@+id/ivEditMemo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/cd_edit_memo"
        android:paddingEnd="@dimen/memo_icon_padding"
        android:paddingStart="@dimen/memo_icon_padding"
        android:src="@drawable/ic_edit_pen" />

    <ImageView
        android:id="@+id/ivDeleteMemo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/cd_delete_memo"
        android:paddingEnd="@dimen/memo_icon_padding"
        android:paddingStart="@dimen/memo_icon_padding"
        android:src="@drawable/ic_delete_memo" />

</LinearLayout>